import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const ScanScreen = () => {
    return (
        <SafeAreaView>
            <Text>Scan Me</Text>
        </SafeAreaView>
    );
};

export default ScanScreen;
